[package]
name = "VKML"
version = "0.1.0"
edition = "2024"

[profile.release]
lto = true

[dependencies]
vulkanalia = { version = "0.28", features = ["libloading"] }
image = { version = "0.25", default-features = false, features = ["png"] }
rand = "0.9"
thiserror = "2"
sys-info = "0.9"